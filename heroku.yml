setup:
  addons:
    -
      as: DATABASE
      plan: heroku-postgresql
build:
  docker:
    db: backend/Dockerfile.db
    web: Dockerfile.web
release:
  command:
    - ./dev-setup.sh
  image: web
run:
  web:
    command:
      - python app.py
    image: web
